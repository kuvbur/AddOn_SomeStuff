# DISCREPANCIES — расхождения комментариев и кода

> Хеш коммита: f8f599c (2026-09-22). Сверено 10 записей (фаза 5). Ничего не исправлялось — только фиксация.

## Найдено в этой сессии

| # | Место | Комментарий/док | Код | Статус |
|---|-------|-----------------|-----|--------|
| 1 | `TestFunc.hpp:14` | `#endif // AC_26` после блока `#ifdef AC_27` | Блок компилируется по AC_27 | Расхождение комментария и кода — комментарий неверен (копипаст) |
| 2 | `Sync.hpp:11` | «Заполнение см. SyncString» для struct SyncRule | Правило заполняется и через `ParseSyncString`, не только `SyncString` | Комментарий неполный |

## Исторические расхождения (из ревью-заметок) — проверены по коду 2026-09-22

| # | Место (было) | Что было | Состояние в коде сейчас |
|---|--------------|----------|------------------------|
| 3 | `Sync.cpp:1997` | `rawName_row_end = rawName_col_start` (copy-paste) | **Не воспроизводится**: строки 2022/2032 — `rawName_row_start/end = paramNamePrefix + dim0/dim1`, симметрично. Исправлено ранее или строки сместились |
| 4 | `Sync.cpp:2632-2644` (SyncGetSubelement) | guid/subguid перепутаны | **Не воспроизводится**: текущий код (Sync.cpp:2649+) — внешний ключ `guid` (родитель), внутренний `subguid`, с комментарием «Внешний ключ — guid (родитель)». Исправлено |
| 5 | `ReNum.cpp:184` | `rule.state = r` (r — указатель, нужно `*r`) | **Не воспроизводится**: ReNum.cpp:181-183 `const RenumRule &rule = *cIt->value;` — корректная ссылка. Исправлено |
| 6 | `Roombook.cpp:2028-2034` | утечка `API_ElementMemo` при `continue` | **Исправлено**: комментарий «FIX (ревью 2026-09-12): утечка memo — Dispose перед continue», `ACAPI_DisposeElemMemoHdls (&memo)` перед continue присутствует |
| 7 | `Helpers.cpp:1761` | `API_ElementMemo memo;` без BNZeroMemory | **Не воспроизводится**: все 7 объявлений в Helpers.cpp — `API_ElementMemo memo = {};` (строки 921, 1184, 2076, 2417, 6482, 9307, 9552). Исправлено |
| 8 | `Dimensions.cpp:158` — `pen_original` | «не чинить без явного запроса» (AGENTS.md §16) | Подтверждено актуально, номер строки сместился: сейчас `pen_original` читается на Dimensions.cpp:49, быстрофикс на :184 (`pen_original = pen_dimenstion; // Быстрофикс`) |


| 9 | `Propertycache.cpp:11` (GetPropertyRuleFlag, #158) | Комментарий: «Парсит описание только при промахе кэша» — подразумевает использование в проде | clangd callHierarchy: все входящие вызовы — только TestFunc (TestGetPropertyRuleFlag ×16, TestPropertyRuleFlagOnProjectElements ×2); в проде функция не вызывается (AC25, compile_commands) | Возможная мёртвая функция в проде — проверить отдельно, не исправлять |

## Итог
- Активных расхождений «комментарий ≠ код» с последствиями не найдено; #1 — косметическое.
- Исторические баги #3–#7 в текущей ветке исправлены.