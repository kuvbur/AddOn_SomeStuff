# P0/P1 — сложные правки и рекомендации по исправлению (SomeStuff addon, ревью 2026-09-12b)

Часть 1 из 2: критические и major-правки + системная S1 (SuspendGroups). Полный список находок — в исходном ревью (review-full-repo-2026-09-12b.md в справочнике навыка archicad-cpp-code-review); часть 2 (minor/PERF) — P2-minor-perf-fixes-2026-09-12b.md.

Ниже сначала — сами находки (что, где, серьёзность), затем рекомендации по правкам для каждой из них.

# Полное ревью Sources/AddOn — SomeStuff addon (2026-09-12, AC25)

Метод: 5 параллельных дочерних ревью (полное чтение файлов) + собственная проверка
SomeStuff_Main.cpp, BrowserPalette.cpp (полное чтение) и кросс-файловых grep-паттернов.
Файлы не редактировались. Предыдущие ревью: dialogs/Sync/ReNum (2026-08-24),
BrowserPalette (2026-09-12) — их находки перепроверены, статусы обновлены ниже.

## Кросс-файловые системные проблемы

### S1 — APITool_SuspendGroups без восстановления (BUG, major)
Тумблер включается при `!suspGrp`, обратного вызова нет. Пользователь остаётся с
выключенной группировкой после команды. Восстановление есть ТОЛЬКО в Sync.cpp:265-277
и ReNum.cpp:149-159. Отсутствует в:
- Summ.cpp:61-66 (SumSelected)
- spec/Spec.cpp:951-956 (SpecArray)
- Roombook.cpp:4121-4125 (Draw_Elements), Roombook.cpp:5286-5290 (SetSyncOtdWall)
SDK: ACAPI_Grouping_Tool/ACAPI_Element_Tool, APITool_SuspendGroups (тумблер, док AC25).

## SomeStuff_Main.cpp — проблем не найдено
TW-фикс (ReservationChangeHandler без записи БД + APINotify_ReceiveChanges) корректен.

## Roombook.cpp (17 находок)
1. BUG critical, Floor_Create_One:2089 — `API_ElementMemo memo;` без `= {}`:
   GetMemo(APIMemoMask_Polygon) заполняет только polygon-поля, Dispose(2185) получает
   мусорные указатели → краш. Фикс: `API_ElementMemo memo = {};` + Dispose перед continue
   при err!=NoError (2095).
2. BUG major, Floor_Create_One:2145-2149 — continue при contourEnds==nullptr без Dispose → утечка.
3. BUG major, OtdWall_Create_FromColumn:1831-1836 — return при err!=NoError без Dispose(segmentmemo).
4. BUG major, Floor_Draw_Slab:4753-4757 — return при err!=NoError без Dispose(memo).
5. BUG major, RoomBook:66-73 — `API_SelectionInfo` не инициализирована; BMKillHandle на
   мусорном marquee.coords при ошибке ACAPI_Selection_Get. Фикс: `= {}`.
6. BUG major, OtdWall_Create_FromWall:1669 — `inxedge > edges.GetSize()` пропускает
   случай ==: чтение за границей. Фикс: `>=` (требует уточнения: фиктивное ребро в конце).
7. BUG major, SuspendGroups без восстановления (см. S1, строки 4121/4125, 5286/5290).
8. BUG minor, Floor_Create_One:2138-2139 — dzUp копипаст dzBottom, обе переменные мёртвые.
9. BUG minor, Param_Material_Get:2742 — API_AttributeIndexFindByName без проверки, при
   ненайденном материале val.intValue получает мусорный индекс.
10. BUG minor, OtdWall_Draw_Object:4460-4486 — dim1 = openings.GetSize(), но отфильтрованные
    continue не попадают в arr_num → dim1*9 != фактический размер.
11. PERF major, RoomBook:567-596 — глубокие копии трёхуровневых HashTable по значению в
    EnumeratePairs (byzone/bytype/byparent) → const&.
12. PERF major, OtdWall_Delim_One (сигнатура 3839) — OtdWall по значению на каждый вызов
    + PushNew копия → const& + std::move.
13. PERF major, Param_SetToRooms/Param_SetToWindows (hpp:346/365) — ReadParams по значению
    на каждую зону/проём; Param_Property_Read мутирует словарь → передать по & или
    готовить рабочую копию один раз.
14. PERF minor, RoomBook:176-181 — копия zoneGuids массива в цикле → const&.
15. PERF minor, Floor_Create_One:2075/2088 — двойная конвертация GUID→string→GUID на каждый
    slab; хранить API_Guid в GuidByZ.
16. PERF minor, RoomBook:521-527, OtdData_WriteToRoom:1082-1084 — ContainsKey+Get → GetPtr.
17. Notes: CollectRoomInfo — утечек НЕТ (zonememo = {}, все пути закрыты). Асимметрия
    10 GetMemo/24 Dispose нормальна (Dispose для GetDefaults/Favorite-memo).

## Helpers.cpp (14 находок)
1. BUG critical, ParamHelpers::WriteGDL:5199-5214 — при err!=NoError от
   GetActParameters выполняется CloseParameters, но return только если закрытие тоже
   упало; иначе err перезаписывается NoError и цикл идёт по мусорному params-хендлу.
   Фикс: безусловный return в блоке ошибки.
2. BUG major, ReadListData:6721-6722 — return при !needListData без BMKillHandle(descRefs).
3. BUG major, GetRelationsElement case API_WallID:973-1016 — fall-through в
   API_RailingID, если !(GetWidoS() && addConnect): GetRElementsForRailing по стене.
   Фикс: break после if-блока.
4. BUG major, ReadElementValues:7228-7231 — case API_ObjectID без break → fall-through в
   API_LampID, material overridden перезаписывается чужой веткой union.
5. BUG major, ReadCoords case API_DoorID:7430 — чтение element.window.openingBase.* для
   двери вместо element.door.openingBase.*.
6. BUG major, ReadMorphParam:1204-1207 — формулы проекций: dlz дублирует dlx, dly из dx —
   {@morph:ly}/{@morph:lz} неверны.
7. BUG major, ReadCoords:2610-2613 — x/l_wall без проверки l_wall==0 → NaN/inf в словаре.
8. BUG major, GetParentGUIDSectElem:768-782 — при err!=NoError выходные parentguid/
   parentType не сбрасываются → вызывающий использует мусор. Фикс: присвоить
   APINULLGuid/API_ZombieElemID на ошибочном пути.
9. BUG major, ReadCoords:2541-2562 — symb_rotangle_axis_fraction/correct/correct_1000
   считаются от slantDirectionAngle вместо axisRotationAngle.
10. BUG minor, ReadCoords:2566/2574 — литерал true вместо 0.0 пятым аргументом
    AddDoubleValueToParamDictValue → записывается 1.0.
11. BUG minor, CallOnSelectedElemSettings:823 / CallOnSelectedElem:865 — (t2-t1)/1000 из
    clock() трактуется как s и ms — единицы неверны.
12. BUG minor, ReadCoords:2402-2413 — `size >= inx_segment` при 0-based segmentID:
    чтение memo.cWallSegments[size] (uncertain, проверить базу индексации в DevKit).
13. PERF minor, ReadIFC:5978-5982 — тройное копирование API_IFCProperty в цикле → const&.
14. PERF minor, ReadGDL:6288 — копия rawName на каждый параметр → const&.
Notes: зона 1761 корректна; все GetMemo/Dispose пары корректны; Helpers.hpp чист.

## CommonFunction.cpp (9)
1. BUG major, ReplaceCR:1052-1064 — ReplaceFirst("\\n") удаляет 2 символа, затем SetChar
   перезаписывает СЛЕДУЮЩИЙ символ: каждый перенос строки съедает один символ данных.
   Фикс: SetChar(inx,CharCR) + Delete(inx+1,1), либо ReplaceFirst(p, UniString(CharCR)).
2. BUG major, GetSelectedElements2:782/790 — API_SelectionInfo без `= {}`; BMKillHandle на
   неинициализированном marquee.coords при ошибке ACAPI_Selection_Get.
3. BUG major, ElemHead_To_Neig:1887-1908 — typeID заполняется только если входной тип
   Zombie; при валидном входном типе функция возвращает false (инвертированная логика).
4. BUG major, ConstructPolygon2DFromElementMemo:2261/2280 — BMGetHandleSize без проверки
   coords/pends на nullptr; -1 попадает в беззнаковый счётчик (uncertain по API).
5. BUG major, ConvertPolygon2DToAPIPolygon:2339 vs 2274 — асимметрия смещения дуг
   (memo копируется с индекса 1, обратно с индекса 0) — смещение на одну позицию
   (uncertain: проверить раскладку ConvertPolygon2DToPolygon2DData).
6. BUG minor, GDLHelpers::ParamToMemo:2608 — BMGetHandleSize(memo.params) без nullptr-проверки.
7. BUG minor, CallOnSelectedElem2:856-857 — clock()/1000 подписано "ms" — единицы неверны.
8. BUG minor, msg_rep:740-742 — при show=true сообщение пишется в отчёт дважды
   (uncertain: уточнить семантику ACAPI_WriteReport по DevKit).
9. PERF minor, GetElementByPropertyDescription:2589 — ToLowerCase() в самом внутреннем
   цикле — вычислить один раз до циклов.

## Propertycache.cpp/.hpp (7)
1. BUG major, GetGroupFromCache:143-175 — после успешного ACAPI_Property_GetPropertyGroup
   group НЕ кладётся в propertygroups → функция всегда false на этом пути и кэш не
   работает (повторные API-вызовы). Фикс: cache.propertygroups.Put(group.guid, group).
2. BUG major, PropertyCache::Update (hpp:310-339) — не очищает compositeCache,
   parsedformatstring, formatstringformeasuretype, file/filedata — устаревшие данные по
   индексам после смены проекта (uncertain: семантика вызова Update).
3. BUG minor, isCacheContainsParamValue:286-315 vs GetParamValueFromCache — ранние
   return false при недоступности одного источника, тогда как Get продолжает по другим —
   противоречивые результаты для одного ключа.
4. BUG minor, ReadLibraryFile:41-56 — GetDataLength до Open, ReadBin без проверки (uncertain).
5. BUG minor, GetAllAttributeToParamDict:672 — err от GetAttributesByType не проверяется:
   true с пустым кэшем.
6. BUG minor, PROPERTYCACHE() синглтон без синхронизации + mutable кэш для JSON-команд —
   возможен race при фоновой обработке команд (uncertain: модель потоков).
7. PERF minor, ReadClassification/ReadInfo/AddFile — ContainsKey+Get → GetPtr.

## ClassificationFunction.cpp (6)
1. BUG major, GetFullName:152-168 — возможная бесконечная рекурсия при совпадении id
   родителя и потомка (uncertain: возможны ли такие данные).
2. BUG minor, GetAllClassification:48-88 — err от последней системы пробрасывается наружу →
   ReadClassification помечает весь кэш нечитанным, хотя остальные системы загружены.
3. BUG minor, FindClass(Pair):219-244 — не вызывает ReadSystemDict перед доступом к кэшу.
4. PERF minor, GetAllClassification:72-80 — две полные глубокие копии ClassificationDict
   (systemname + systemname_full) + третья для autoclassifications → std::move для второй.
5. PERF minor, AddClassificationItem:138-139 — desc уже lower, ToLowerCase() трижды в
   Contains-условиях.
6. PERF minor, GetFullName — до 6-7 lookup хэш-таблицы на уровень рекурсии → GetPtr.
SetAutoclass: 2 ACAPI-вызова на элемент — оценить точки вызова (uncertain).

## Summ.cpp (7)
1. BUG major, SumSelected:61-63 — SuspendGroups без восстановления (см. S1).
2. BUG major, SumSelected:57-79 — код ACAPI_CallUndoableCommand игнорируется:
   при откате WriteInfo/SyncArray/отчёт выполняются как при успехе.
3. BUG minor, Sum_OneRule:432/445/452 — bool min/max агрегация через ||/&& инвертирована
   (min=bool AND, max=bool OR), для NUM_SUM бессмысленно (uncertain: нужны ли bool вообще).
4. PERF major, Sum_GetElement:252-259 — ACAPI_Element_Filter в цикле по элементам ВНУТРИ
   цикла по правилам: O(правила×элементы) — вынести фильтрацию на элемент.
5. PERF minor, Sum_GetElement:203-208 — копия API_PropertyDefinition в цикле → const&.
6. PERF minor, Sum_GetElement:209-217 — тройной lookup (ContainsKey, ContainsKey, Get) → GetPtr.
7. PERF minor, Sum_OneRule:398/478/484 — копия eleminpos на группу; копия ParamValue на
   элемент; ToString(summ) в цикле записи хотя summ не меняется.

## Dimensions.cpp (6)
1. PERF major, DimRoundAll:326-343 — полный пересчёт всех размеров проекта на каждое
   событие EndEvents/ChangeWindow/ChangeFloor/ChangeProjectDB и каждую команду меню
   (SomeStuff_Main.cpp:109/142/436). 2 API-вызова на размер. Фикс: убрать из
   MenuCommandHandler для команд не про размеры; в EndEvents — адресно через
   DimAutoRoundOne (уже существует); кэшировать наличие правил (hasDimAutotext).
2. BUG major, DimRoundAll isUndo=true (332-339) — ACAPI_Element_Change размеров вне
   undoable-команды (реальный путь из ElementEventHandlerProc EndEvents) (uncertain по
   контексту TW-транзакции).
3. PERF major, DimParse:257 — копирование всего paramDict (HashTable<UniString,ParamValue>)
   на каждый размер × каждое правило. Копия нужна только ради measuredvalue — передать
   значение отдельным параметром.
4. BUG minor, DimAutoRound:141-148 + DimParse:300-319 — content мутируется первым правилом,
   второе правило сравнивает custom_txt с текстом от первого, а не с исходным.
5. BUG minor, DimAutoRoundOne checktype=false вызывается и для Zombie/Group из
   ElementEventHandlerProc:155 — чтение union element.dimension по не-размеру (uncertain).
6. BUG minor, DimAutoRound:54-71 — pen-правило (ключ-число) может ложно совпасть как
   layer-regex → правило добавляется дважды (uncertain).

## ResetProperty.cpp (6)
1. BUG major, ResetPropertyElement2Defult:88 (AC25-ветка #else) — ACAPI_Database
   (APIDb_ChangeCurrentDatabaseID, &commandID) передаёт API_DatabaseID* (4 байта) вместо
   API_DatabaseInfo* — возврат в исходную БД никогда не работает, восстановление
   комбинации слоёв пропускается. Верифицировано по DevKit-25 Database_Control.cpp.
   Фикс: сохранить API_DatabaseInfo origDB через APIDb_GetCurrentDatabaseID и
   восстанавливать её.
2. BUG minor, ResetOneElemenDefault:381 — AC26+ ветка передаёт полный properties вместо
   properties_to_reset (рассинхрон с AC25-веткой).
3. BUG minor, ResetPropertyElement2Defult:40-113 — массовые SetProperties/SetPropertiesOfDefaultElem
   вне ACAPI_CallUndoableCommand → десятки шагов undo (uncertain).
4. PERF minor, :63 и :81 — ResetElementsInDB вызывается дважды с одинаковыми аргументами.
5. PERF minor, :273/297/303 — ResetOneElemenDefault(API_ObjectID,...) трижды с идентичными
   аргументами.
6. PERF minor, :245 — копия API_Property на каждое свойство → ссылка + std::move.

## Spec.cpp (7)
1. BUG major, SpecArray:701-706 — range-for по exsist_elements, но тело использует
   exsist_elements[i] с посторонней переменной i (=2..3 от фаз прогресса): один и тот же
   случайный индекс, выход за границы при малом массиве. Фикс: exsist_element в 703/705.
2. BUG major, SpecArray:951-957 — SuspendGroups без восстановления (см. S1).
3. BUG minor, :754 — `return false;` в GSErrCode → NoError, отмена пользователя = успех.
   Фикс: APIERR_CANCEL.
4. BUG minor, :861-864/1754-1757/2444-2447 — смешение APIGuidToString и
   APIGuid2GSGuid().ToUniString() в одной sync_guid-строке (uncertain: идентичны ли форматы).
5. BUG minor, GetSizePlaceElement:2316-2318 — BMGetHandleSize(memot.params) без
   nullptr-проверки (uncertain).
6. PERF minor, :624-641 — ContainsKey+Get по paramdict_favorite в циклах → GetPtr.
7. PERF minor, :1452 — ACAPI_Element_Filter в цикле по elements, не зависит от группы —
   вынести.

## Spec_libpart.cpp (2)
1. BUG major, AddMat/AddArm/AddSubpos/AddProkat (110-273) — off-by-one guards: `<10` читает
   [10]; `<8` читает [9]; `<9` читает [9]; `<14` читает [14]. Фикс: `<11`, `<10`, `<10`, `<15`.
2. BUG major, AddMat:110/115 — локальная `GS::UniString subpos` затеняет внешнюю →
   GetSubposKey всегда получает пустую строку: материалы v3 смешиваются между позициями.

## Revision.cpp (7)
1. BUG major, GetAllChangesMarker:233-375 — API_LayoutInfo не инициализирована; нет
   nullptr-проверки customData перед ContainsKey/Set/Put (в ChangeLayoutProperty проверка
   есть) → краш на макете без customData. Фикс: `= {}` + `if (customData==nullptr) continue;`.
2. BUG major, GetMarkerPos:871-872 — разыменование memo.pends/coords без nullptr-проверки и
   без проверки begInd против размера coords.
3. BUG major, ChangeMarkerText:1012/1020 — GS::ucscpy в value.uStr без обрезки по
   API_UAddParStrLen — переполнение фиксированного буфера (в Spec.cpp:2525 корректно
   обрезается). Фикс: обрезать как в Spec.cpp.
4. BUG major, GetScheme:94-111 — layout_note_guid.Add по ключу «somestuff_...»: если имя
   содержит somestuff_layout_type/shifr, оно добавляется дважды → Add с существующим
   ключом = ассерт/краш (uncertain: допускает ли GS::HashTable::Add дубли).
5. BUG minor, GetChangesLayout:779 — std::atoi: ошибка разбора неотличима от кода 0.
6. BUG minor, GetMarkerText:899/ChangeMarkerText:1004 — BMGetHandleSize(memo.params) без
   nullptr-проверки (uncertain).
7. BUG minor, :628-634/947-951 — `for (auto ch : changes)` копирует пары; в AC28+-ветке
   модификации по ссылке на копию теряются (для AC25 — ок). Фикс: `auto &ch`.

## AutomateFunction.cpp (6)
1. BUG major, AlignOneDrawingsByPoints:763-764/791-792 — ранние return после смены БД без
   возврата в databasestart → последующие чертежи обрабатываются в чужой БД.
2. BUG major, GetSectLine:326 — разыменование memo.elemInfoString без nullptr-проверки.
3. BUG minor, AlignDrawingsByPoints:914/935/944 — return без снятия StoreViewSettings
   (восстановление только в 969-976).
4. BUG minor, GetCuplane:68-74 — при dx≈0 и dy≈0 x перезаписывается, isx не сбрасывается
   (uncertain: intended-поведение).
5. PERF minor, GetSectLine:331-343 — p.Count(p[i]) в двойном цикле O(n²) → HashSet.
6. PERF minor, AlignOneDrawingsByPoints:775-790 — ACAPI_Element_Get на каждый хотспот;
   прервать при нахождении обеих крайних точек.

## MEPv1.cpp (только AC27+, для AC25 не компилируется) — 3
1. BUG major, GetMEPData:360 — systems.UnwrapErr() на успешном Result — UB: читается на
   каждом MEP-элементе вне физсистем.
2. BUG minor, GetSubElement:240-242 — err от ACAPI_Element_GetHeader не проверяется.
3. PERF minor, :152-153/168-169 — Put + GetPtr двойной lookup.

## BrowserPalette.cpp — перепроверено мной (2026-09-12b)
Подтверждено из ревью 2026-09-12 (без изменений в коде):
1. BUG major, HighlightElements:485/516 — suppressSelectionRefresh не exception-safe
   (нет try/catch, в отличие от соседей): исключение оставляет флаг true навсегда.
2. BUG major, :186-192/115/1188 — JS-объект ре-регистрируется на каждой загрузке страницы,
   UnregisterJSObject никогда не вызывается; RegisterAsynchJSObject вернул bool — не
   проверяется (1188).
3. BUG major/perf, Show():115 + HidePalette_End:1284-1287 — ReloadIgnoreCache на каждый
   показ палитры: сброс состояния страницы + запись настроек.
4. BUG minor, GetPropertiesList:331 — count = selectedElements.GetSize(), хотя элементы
   с err/пустыми definitions пропущены continue → count больше фактического.
5. PERF major, GetPropertiesList:214-265 — полный обход определений/значений через API
   для каждого элемента (кэш selectionPropertiesCache обходится) +
   ACAPI_Property_GetPropertyGroup на каждое свойство каждого элемента — O(N·M) API-вызовов.
6. BUG minor, GetPropertyValue:438 — propertyName содержит GUID, а не имя.
7. BUG minor, GetPropertyValue:435 — common=false при пустом выделении (семантика).
8. BUG minor, GetClassification: смешанные типы ответа — JSON-строка (1014) vs
   DG::JSObject с вложенными JSArray (778-784, 1017-1035) — вложенные структуры через
   CEF не доходят (HTML защищается).
9. BUG minor, GetClassification:961-968/989-993 — Int32 cnt/tot/count/total читаются
   ObjectState::Get без инициализации.
10. BUG minor, HighlightElements/ParsePropertyDescription/ParsePropertyForElement/
    GetPropertiesList/GetPropertyValue/GetSelectionInfo — лямбды без try/catch
    (неоднородно с GetClassification/SetClassification).

Новые мои находки:
11. BUG major, FilterElementsByType:1234-1249 — ACAPI_Element_Get (полный элемент) на
    каждый GUID выделения в каждой bridge-функции; для фильтра типа достаточно
    ACAPI_Element_GetHeader. На выделении в тысячи элементов — тысячи тяжёлых Get.
    SDK: ACAPI_Element_Get/GetHeader.
12. BUG minor, HighlightElements:490-491 — hlElems.Add(guid, color) в цикле: дубликаты
    GUID во входном JSON → Add с существующим ключом (GS::HashTable::Add — дубли
    запрещены) → ассерт/краш. Фикс: Put вместо Add (дешевле и безопаснее).
13. BUG minor, GetPropertiesList:253-264 — group.name читается в DBprnt (261) ДО проверки
    groupErr: при неудачном ACAPI_Property_GetPropertyGroup читается неинициализированная
    UniString (живой путь в TESTING-сборке).
14. BUG minor, UpdateSelectionInfoInUI:147-148 — при пустом выделении функция выходит
    ДО ExecuteJS: UI палитры не обновляется до «0 элементов» при сбросе выделения —
    счётчик остаётся устаревшим. Фикс: пушить refresh с count=0 до раннего return.

## Сводка
critical: 2 (Roombook Floor_Create_One memo; Helpers WriteGDL)
major: ~30 | minor: ~40 | uncertain (требуют уточнения): 18
SuspendGroups — системная проблема в 5 местах (исправлена только в Sync/ReNum).

---

# Рекомендации по правкам (по каждой находке)

Формат: **Правка** (что и где менять) → **Проверка** (как убедиться).
Общие правила для всех правок: clang-format только по редактируемому файлу, после
каждого бага с воспроизводимым эффектом — TDD-цикл по схеме Дмитрия (GREEN-регрессии +
RED на баг → фикс → зелёные; прогон через restart_archicad_for_test.ps1, анализ
test_results.txt). Прежде чем трогать SDK-вызов — верификация сигнатуры через
LightRAG/DevKit-25 (не по памяти).

## P0 — критические (чинить первыми, каждый — кандидат RED-теста)

### Helpers.cpp WriteGDL:5199-5214
**Правка:** в блоке `if (err != NoError)` после `ACAPI_LibraryPart_CloseParameters()`
сделать return безусловным:
```cpp
if (err != NoError) {
    msg_rep (...);
    ACAPI_LibraryPart_CloseParameters ();
    return;                      // было: return только при ошибке закрытия
}
```
**Проверка:** RED-тест — библиотечный элемент с битой библиотекой (GetActParameters
падает): до фикса чтение мусорного params-хендла, после — чистый выход с msg_rep.
Билд + прогон в AC с отсутствующим/повреждённым параметром GDL.

### Roombook.cpp Floor_Create_One:2089
**Правка:** `API_ElementMemo memo;` → `API_ElementMemo memo = {};` (это ловушка
BNZeroMemory из AGENTS.md — не убирать и в других местах). Плюс два пути утечки:
- 2095 (err != NoError после GetMemo): добавить `ACAPI_DisposeElemMemoHdls (&memo);`
  перед `continue` — при частичной аллокации SDK мог уже выделить хендлы;
- 2145-2149 (contourEnds == nullptr): добавить Dispose после FreePolygon2DData, перед
  `continue`.
**Проверка:** RED-тест на отделку перекрытия в комнате без пандусов (Floor_Create_One);
в TESTING-сборке под счётчик аллокаций (или просто стабильность на 20+ перекрытиях —
до фикса краш на мусорных хендлах в Dispose).

## P1 — major, краш-риски на штатных данных (минимальные правки, высокая отдача)

### Roombook.cpp OtdWall_Create_FromColumn:1831-1836
**Правка:** перед `return` в ветке `err != NoError || segmentmemo.columnSegments ==
nullptr` добавить `ACAPI_DisposeElemMemoHdls (&segmentmemo);`. Надёжнее — вынести
Dispose на общий выход функции (RAII-обёртка ElemMemoGuard, как рекомендует
bug-pattern-catalog), но минимально хватит Dispose в каждой ранней ветке.
**Проверка:** тест на колонну с отделкой (OtdWall из колонны); прогон 2-3 раза — нет
роста памяти в мониторе ArchiCAD.

### Roombook.cpp Floor_Draw_Slab:4753-4757
**Правка:** аналогично — `ACAPI_DisposeElemMemoHdls (&memo);` перед return при
err != NoError. **Проверка:** тест с этажом, где есть плита без полигона (повреждённая).

### Roombook.cpp RoomBook:66-73 и CommonFunction.cpp GetSelectedElements2:782/790
**Правка:** в обоих местах `API_SelectionInfo selectionInfo;` → `= {};`. Это одна
и та же ловушка в двух файлах — править парой.
**Проверка:** GREEN-тест на выбор (DBtest → выбор элементов → count совпадает);
краш воспроизводится только на ошибке Selection_Get — достаточно код-ревью + `={}`.

### Helpers.cpp GetRelationsElement:973-1016
**Правка:** добавить `break;` после закрывающей скобки
`if (syncSettings.GetWidoS() && addConnect) { ... }` перед `case API_RailingID:`.
**Проверка:** RED-тест: стена с выключенной синхронизацией окон (SetWidoS=false):
до фикса в rereadelem попадают railing-элементы стены, после — нет.

### Helpers.cpp ReadElementValues:7228-7231
**Правка:** `break;` после ConvertBoolToParamValue для API_ObjectID.
**Проверка:** RED-тест: объект с useObjMaterials=false и лампа с true в одном
выделении — до фикса {@material overridden} у объекта принимает значение лампы.

### Helpers.cpp ReadCoords Door:7430
**Правка:** в case API_DoorID заменить `element.window.openingBase.width/height` на
`element.door.openingBase.width/height`.
**Проверка:** LightRAG-проверка раскладки union (API_DoorType/API_WindowType в
APIdefs_Element... не assumed). RED-тест: дверь с проёмом — {@width} до фикса =
значение соседнего окна/мусор union, после — ширина двери.

### Helpers.cpp ReadMorphParam:1204-1207
**Правка:** согласовать оси (проверить по назначению параметров в GDL-скрипте):
dlx = sqrt(dx*dx+dy*dy), dly = sqrt(dy*dy+dz*dz), dlz = sqrt(dx*dx+dz*dz) — минимум
устранить дублирование dlz==dlx. Это фактически доработка формул — согласовать с
Дмитрием, что задумано ({@morph:l*} = проекции рёбер на плоскости).
**Проверка:** RED-тест на морф с известным ребром (1000×500×300) — проверка
{@morph:lx},{@morph:ly},{@morph:lz} против ручного расчёта.

### Helpers.cpp ReadCoords:2610-2613
**Правка:** перед делением: `if (is_equal (l_wall, 0.0)) continue;` (пропустить
вычисление середины проёма) — нулевая стена это вырожденный случай, безопаснее
пропустить, чем писать NaN в словарь (NaN дальше заражает все агрегаты).
**Проверка:** GREEN-тест на вырожденную стену (begC==endC): до фикса свойства
середины проёма = NaN, после — отсутствуют.

### Helpers.cpp GetParentGUIDSectElem:768-782
**Правка:** в начале функции: `*parentguid = APINULLGuid; *parentType =
API_ZombieElemID;` (сигнатуры уточнить — там указатели/ссылки), и ранний return при
err != NoError от ACAPI_Element_Get до чтения подструктур. Соответствует
bug-pattern-catalog «ACAPI_Element_Get return as a contract».
**Проверка:** RED-тест: открытие окна разреза создаёт виртуальный zombie-элемент —
до фикса наблюдатель может получить мусор parentType, после — ZombieElemID и ранний
выход вызывающего.

### Helpers.cpp ReadCoords:2541-2562
**Правка:** в 2543 передать `axisRotationAngle` вместо `slantDirectionAngle` в
CoordCorrectAngle для symb_rotangle_axis_fraction/correct/correct_1000.
**Проверка:** RED-тест: наклонённая колонна с разными slant и axis углами —
{@symb_rotangle_slant_fraction} ≠ {@symb_rotangle_axis_fraction} после фикса
(до фикса они равны).

### CommonFunction.cpp ReplaceCR:1052-1064
**Правка:** минимально — заменить пару ReplaceFirst+SetChar на
`val.ReplaceFirst (p, GS::UniString (CharCR));` (без SetChar вовсе). Если ReplaceFirst
не принимает UniString-замену в AC25 (проверить сигнатуру в LightRAG): SetChar(inx,
CharCR) затем val.Delete(inx+1, 1) — сначала перезаписать 'n', потом удалить '\\'.
**Проверка:** RED-тест: строка "a\\ndef" → "a<CR>def" (символ 'd' не теряется).
Тест обязателен: ReplaceCR вызывается из форматирования значений — регресс легко
не заметить.

### CommonFunction.cpp ElemHead_To_Neig:1887-1908
**Правка:** инициализировать typeID из заголовка ДО if:
AC26+ ветка: `typeID = elemHead->type.typeID;`, старая: `typeID =
elemHeadNonConst->typeID;` — и лишь при `typeID == API_ZombieElemID` дополнять через
ACAPI_Element_GetHeader.
**Проверка:** сначала grep все точки вызова (в каком виде туда приходит elemHead —
заполненный тип или пустой): если ВСЕ вызовы передают незаполненный заголовок,
текущий код «работает» и правка может изменить поведение — тогда только добавка
заполнения при валидном входе, без удаления zombie-ветки. Отметить как «требует
уточнения», если вызовы неоднородны.

### CommonFunction.cpp ConstructPolygon2DFromElementMemo:2261/2280 + ConvertPolygon2D:2339
**Правка:** перед вычислением nVertices/nContours:
`if (memo.coords == nullptr || BMGetHandleSize((GSHandle)memo.coords) < (GSSize)sizeof(API_Coord)) return APIERR_BADPARS;`
и аналогично для memo.pends. По смещению дуг (2339): сверить раскладку
Geometry::ConvertPolygon2DToPolygon2DData в DevKit-25 (поля API_Polygon arcs с индекса
0 или 1) — если с 1, копировать `*polygon2DData.arcs + 1`.
**Проверка:** LightRAG/DevKit по ConvertPolygon2DToPolygon2DData; RED-тест: элемент
без полигона (memo.coords == nullptr) — до фикса краш/гигантская аллокация, после
APIERR_BADPARS.

### Propertycache.cpp GetGroupFromCache:143-175
**Правка:** после успешного ACAPI_Property_GetPropertyGroup:
`cache.propertygroups.Put (group.guid, group);` — до финального GetPtr. Заодно
пересмотреть условие ветления L145/L151 (isGroupPropertyRead vs _full).
**Проверка:** RED-тест: первое обращение к группе (кэш пуст) — до фикса функция
возвращает false, после — true и второй вызов не делает API-запрос (DBprnt счётчик).

### Spec.cpp SpecArray:701-706
**Правка:** в теле range-for заменить `exsist_elements[i]` на `exsist_element`
(строки 703 и 705). Переменную i не трогать (используется фазами прогресса).
**Проверка:** RED-тест: правило с 3+ существующими spec-элементами — до фикса в
rule.exsist_elements один дублированный элемент (или краш при <3), после — все.

### Spec_libpart.cpp AddMat/AddArm/AddSubpos/AddProkat (110-273)
**Правка:** guards: AddMat `<10`→`<11`; AddArm `<8`→`<10` (читается [9]);
AddSubpos `<9`→`<10`; AddProkat `<14`→`<15`. Внимание: guard AddArm держит [8] И [9]?
перечитать фактические максимальные индексы в теле каждой функции и ставить guard =
maxIndex+1.
**Проверка:** RED-тест: строки libpart ровно с N полями — до фикса ассерт GS::Array
при size==N, после — корректный разбор. Плюс отдельный тест AddMat v3: subpos-ключ
не пустой (см. следующий пункт).

### Spec_libpart.cpp AddMat:110/115
**Правка:** `GS::UniString subpos = partstring[0];` → `subpos = partstring[0];`
(убрать локальное объявление). Компилятор подсветит, если конфликт имён.
**Проверка:** RED-тест: два материала v3 из разных позиций — до фикса оба получают
пустой subpos-ключ и смешиваются, после — разные ключи.

### ResetProperty.cpp:88
**Правка:** в AC25-ветке сохранить исходную БД и восстанавливать её:
```cpp
API_DatabaseInfo origDB = {};
ACAPI_Database (APIDb_GetCurrentDatabaseID, &origDB, nullptr);
... // работа
ACAPI_Database (APIDb_ChangeCurrentDatabaseID, &origDB, nullptr);
```
Переменную commandID (API_DatabaseID) для ChangeCurrentDatabaseID не использовать.
Учесть: после восстановления БД восстановление комбинации слоёв (94-101) начнёт
наконец выполняться (err==NoError) — проверить, что оно корректно (до сих пор было
мёртвым кодом на этом пути).
**Проверка:** ручной тест в AC25: ResetProperty на проекте с разрезами/фасадами →
после команды активная БД и комбинация слоёв = исходные (до фикса — последний
обработанный разрез). Проверено по DevKit-25 Database_Control.cpp.

### Revision.cpp GetAllChangesMarker:233-375
**Правка:** `API_LayoutInfo layoutInfo = {};` + после GetLayoutSets:
`if (layoutInfo.customData == nullptr) continue;` — копией паттерна
ChangeLayoutProperty:398-401.
**Проверка:** ручной тест: макет без customData (созданный не аддоном) в наборе —
до фикса краш при открытии макета, после — пропуск.

### Revision.cpp GetMarkerPos:871-872
**Правка:**
```cpp
if (memo.pends == nullptr || memo.coords == nullptr ||
    BMGetHandleSize ((GSHandle)memo.coords) / sizeof (API_Coord) <= begInd) {
    ACAPI_DisposeElemMemoHdls (&memo);
    return false;
}
```
**Проверка:** ручной тест: маркер ревизии на макете с урезанным полигоном; основной
страховкой остаётся nullptr-проверка.

### Revision.cpp ChangeMarkerText:1012/1020
**Правка:** обрезать как в Spec.cpp:2525:
```cpp
GS::ucscpy ((*memo.params)[i].value.uStr,
            nuch.ToUStr (0, GS::Min (nuch.GetLength (), (USize)API_UAddParStrLen)).Get ());
```
(обе строки 1012 и 1020). **Проверка:** RED-тест: текст изменения >255 символов —
до фикса переполнение буфера, после — обрезанный текст без повреждения кучи.

### AutomateFunction.cpp AlignOneDrawingsByPoints:763/791
**Правка:** перед обоими ранними return восстановить БД (и окно) — скопировать блок
восстановления из конца функции (813-829), либо ввести флаг и единый выход. При
правке: в 763/791 дополнительно восстановить store-настройки, если менялись (см.
следующий пункт).
**Проверка:** ручной тест: чертёж с 0 хотспотов в середине списка — после фикса
СЛЕДУЮЩИЕ чертежи выравниваются корректно (до фикса — обрабатываются в чужой БД).

### AutomateFunction.cpp GetSectLine:326
**Правка:** `if (memo.elemInfoString == nullptr) { ACAPI_DisposeElemMemoHdls (&memo);
return APIERR_GENERAL; }` перед `id = *memo.elemInfoString;`. Плюс раннюю проверку
типа (Railing/Morph) сделать ДО ACAPI_Element_GetMemo.
**Проверка:** ручной тест: элемент не-Railing/Morph в выделении.

### BrowserPalette.cpp HighlightElements:485/516
**Правка:** обернуть тело между 485 и 516 в try/catch с
`suppressSelectionRefresh = false` в catch (перевыбросить после сброса), либо RAII:
```cpp
struct SuppressGuard {
    ~SuppressGuard () { BrowserPalette::suppressSelectionRefresh = false; }
} guard;
```
Заодно (та же функция, 490-491): `hlElems.Add` → `hlElems.Put` — дубликаты GUID из
JSON больше не ассертятся.
**Проверка:** существующий сценарий подсветки (коммит 16f2b82) — регресс: выделение
не сбрасывается; отдельный тест с дубликатом GUID в JSON.

### BrowserPalette.cpp ре-регистрация JS-объекта (186-192/1188)
**Правка:** в RegisterACAPIJavaScriptObject перед созданием нового объекта:
`browser.UnregisterJSObject ("ACAPI");` (перегрузка по имени, верифицирована в
DGBrowser.hpp через LightRAG); проверить bool RegisterAsynchJSObject и логировать
отказ через DBprnt.
**Проверка:** ручной тест: 10× открыть/закрыть палитру + вызвать GetSelectionInfo —
мост живой, нет дублирующихся функций в window.ACAPI (проверка в консоли CEF).

### BrowserPalette.cpp Show():115 (ReloadIgnoreCache)
**Правка:** минимально — убрать `browser.ReloadIgnoreCache()` из Show(); HTML уже
грузится в InitBrowserControl при создании, а onLoadingStateChange регистрирует мост.
Если рестарт страницы действительно нужен для применения новых настроек — выполнять
reload только при явном изменении (флаг «settings changed»), не на каждый
HidePalette_End. Согласовать с Дмитрием: возможно reload добавлен сознательно для
обновления интерфейса (в ТЗ) — пометить «требует уточнения» перед удалением.
**Проверка:** ручной тест: спрятать/показать палитру (модальная операция) — активная
вкладка, фильтр и прокрутка сохраняются (до фикса сбрасывались).

### Summ.cpp SumSelected:57-79
**Правка:**
1. `GSErrCode undoErr = ACAPI_CallUndoableCommand (undoString, [&] ...);` — при
   `undoErr != NoError`: msg_rep и выход ДО WriteInfo/SyncArray (не выполнять
   пост-шаги как при успехе).
2. SuspendGroups-восстановление по образцу Sync.cpp:265-277 (см. S1 ниже).
**Проверка:** ручной тест: SumSelected при заблокированном слое (запись падает) —
отчёт не врёт про успех. Плюс тест на группу: после SumSelected группировка двигается
целиком.

### Dimensions.cpp DimRoundAll (isUndo, hot paths)
**Правка (по шагам, минимально):**
1. SomeStuff_Main.cpp MenuCommandHandler: DimRoundAll вызывать только для команд,
   меняющих размеры/свойства (SyncAll, SyncSelect, ReNum, Sum, Spec, Roombook) —
   не для переключателей флагов и Pallete.
2. ElementEventHandlerProc EndEvents (Main:142): вместо полного скана — адресная
   обработка изменённых элементов (DimAutoRoundOne уже вызывается в наблюдателе
   элементов; полный скан оставить только под hasDimAutotext-флагом).
3. isUndo=true (Dimensions.cpp:332-339): обернуть ACAPI_Element_Change в
   ACAPI_CallUndoableCommand — согласовать с Дмитрием: сейчас это сознательная
   оптимизация против «лишнего» undo-шага? Если да — пометить «требует уточнения»
   и не оборачивать без решения.
**Проверка:** замер времени переключения этажа на большом проекте до/после; в
тестах — размеры округляются по-прежнему (GREEN-регрессия DimRound).

## S1 — SuspendGroups: одна правка для 4 файлов
**Правка (единый паттерн, копия Sync.cpp:265-277):** после записи внутри лямбды
(или после CallUndoableCommand в Roombook):
```cpp
if (!suspGrp) {
    bool suspNow = false;
    // AC27+: ACAPI_View_IsSuspendGroupOn (&suspNow)
    // AC25:  ACAPI_Environment (APIEnv_IsSuspendGroupOnID, &suspNow, nullptr)
    if (... == NoError && suspNow)
        ACAPI_Grouping_Tool / ACAPI_Element_Tool (guids, APITool_SuspendGroups, nullptr);
}
```
Места: Summ.cpp:61-66, Spec.cpp:951-956, Roombook.cpp:4121-4125 (Draw_Elements —
восстановление после undoable-команды), Roombook.cpp:5286-5290 (SetSyncOtdWall —
после цикла записи, один раз, не в каждой итерации).
Учесть: в Roombook SetSyncOtdWall suspend вызывается в цикле на каждую зону —
сначала убедиться, не является ли повторный SuspendGroups для следующей итерации
«восстановлением» предыдущей (двухфазный тумблер); тогда правка иная: вынести
suspend/restore за цикл целиком. **Перед правкой пометить к проверке по факту.**
**Проверка:** ручной тест на каждом пути: Sum/Spec/RoomBook на сгруппированных
элементах → после команды группы редактируются целиком (SuspGrp-переключатель в AC
выключен).
